client_max_body_size 10M;

upstream web {
  server backend:8000;
}

server {
    listen 8080;
    server_name localhost;

    location /static/ {
        autoindex on;
        alias /static/;
    }

    location / {
        proxy_pass http://web/;
    }
}